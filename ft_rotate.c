#include "ft_push_swap.h"

// ra: a의 모든 요소 위로 로테이션
// rb: b의 모든 요소 위로 로테이션
// rr: ra와 rb 동시 수행

void			ra_rb(t_list *stack)
{
	// 헤더는 두번째 노드를 가리키고 
	// 두번째 노드는 헤드를 가리킴

	// 테일은 첫번째 노드를 가리키고
	// 첫번째 노드는 테일을 가리킴
}